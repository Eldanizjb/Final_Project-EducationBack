@model Subscribe

@{
    ViewData["Title"] = "SubscribeDetail";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml";
}



@{
    if (TempData["SubscribeError"] != null)
    {
        <div class="alert alert-success">
            <p class="text-danger">@(TempData["SubscribeError"] as string)</p>
        </div>
        TempData["SubscribeError"] = null;
    }
}


<!-- partial -->
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <div asp-validation-summary="All" class="text-danger"></div>

                        <div class="form-group">
                            <div class="col-lg-12">
                                <h1 class="card-title" style="color:darkred">Content</h1>
                                <textarea class="form-label">@Html.Raw(Model.Email)</textarea>
                            </div>
                        </div>
                      
                        <a class="btn btn-success" asp-area="admin" asp-controller="Subscribe" asp-action="Update" asp-route-id="@Model.Id">Update</a>
                        <a class="btn btn-danger" asp-area="admin" asp-controller="Subscribe" asp-action="Delete" asp-route-id="@Model.Id" onclick="return confirm('Silinməsinə əminsənmi?')">Delete</a>
                        <a class="btn btn-secondary" asp-area="admin" asp-controller="Subscribe" asp-action="Index"> Back to list</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
